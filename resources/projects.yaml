# Tracked OpenStack packages. The cycle scope (Hibiscus, noble, hibiscus
# pocket) is baked into `wp_template.yaml`, not configured here.

projects:
  - package: cinder
  - package: python-novaclient
    openstack_series: gazpacho
    ubuntu_series: noble
