# Provenance

Real packastack build output captured 2026-04-14. Source:

    ~/.cache/packastack/build/ironic-ui/20260414-143601/build-output/

Used as a known-good input for the QA validator skill and any downstream
stage tests. Do not modify — replace wholesale if a fresher reference build
is needed.

Files include the binary `.deb`, source dsc + tarballs, build/buildinfo/
changes records, and the upstream signature. This is what a successful
packastack run produces; the QA skill's job is to inspect these and produce
a verification stamp.
